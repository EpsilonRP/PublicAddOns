---@class ns
local ns = select(2, ...)

local Constants = ns.Constants

-- // DEPRECATED

---@class Utils_NineSlice
ns.Utils.NineSlice = {
	--[[
	ApplyLayoutFromTex = ApplyLayoutFromTex,
    ApplyLayoutByName = ApplyLayoutByName,
    AddLayout = AddLayout,
	--]]

	--ApplyLayoutFromTex = ApplyLayoutFromTex,
	--ApplyLayoutByName = NineSliceUtil.ApplyLayoutByName,
	--AddLayout = NineSliceUtil.AddLayout,
}
